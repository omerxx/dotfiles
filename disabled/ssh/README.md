# ssh/

## Purpose
SSH client host and connection configuration.

## Contents
- `ssh-config` — SSH configuration file.

## Current Stow target result
- `~/.config/ssh-config`

## Usage status
- Likely unused by default OpenSSH, which expects `~/.ssh/config`.
