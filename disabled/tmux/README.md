# tmux/

## Purpose
tmux session configuration and helper utilities.

## Contents
- `tmux.conf` — Primary tmux config.
- `tmux.reset.conf` — Alternate/reset config profile.
- `scripts/cal.sh` — Helper script.
- `README.md` — Directory documentation.

## Current Stow target result
- `~/.config/tmux.conf`
- `~/.config/tmux.reset.conf`
- `~/.config/scripts/cal.sh`

## Usage status
- tmux usually expects `~/.tmux.conf` by default, so this may require manual `-f` usage or extra symlinking.
