# nix/

## Purpose
Nix configuration.

## Contents
- `nix.conf` — Nix settings.

## Current Stow target result
- `~/.config/nix.conf`

## Usage status
- Potentially mis-targeted. Common locations are `/etc/nix/nix.conf` or `~/.config/nix/nix.conf`.
