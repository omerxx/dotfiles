# gh-dash/

## Purpose
Configuration for `gh-dash` (GitHub terminal dashboard).

## Contents
- `config.yml` — Dashboard layout and behavior.

## Current Stow target result
- `~/.config/config.yml`

## Usage status
- Potentially mis-targeted. Most tools expect namespaced paths (for example under `~/.config/gh-dash/`).
