# atuin/

## Purpose
Configuration for Atuin shell history/search.

## Contents
- `config.toml` — Atuin settings.

## Current Stow target result
- Intended by package: `~/.config/config.toml`

## Usage status
- **Currently unused by setup** because `.stowrc` ignores `atuin/*`.
- Even if not ignored, Atuin usually expects `~/.config/atuin/config.toml`.
