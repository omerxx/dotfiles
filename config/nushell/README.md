# nushell/

## Purpose
Nushell configuration.

## Contents
- `config.nu` — Main Nushell config.
- `env.nu` — Environment setup for Nushell.

## Current Stow target result
- `~/.config/config.nu`
- `~/.config/env.nu`

## Usage status
- Potentially mis-targeted; Nushell often expects these under `~/.config/nushell/`.
