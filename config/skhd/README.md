# skhd/

## Purpose
Hotkey mappings for `skhd`, including automation AppleScripts.

## Contents
- `skhdrc` — Main keyboard shortcut config.
- `applescripts/` — Script helpers invoked by keybinds.

## Current Stow target result
- `~/.config/skhdrc`
- `~/.config/applescripts/...`

## Usage status
- Might be valid only if `skhd` is launched with an explicit config path.
- Default setups often use `~/.skhdrc`.
