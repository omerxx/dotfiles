# zellij/

## Purpose
Zellij terminal multiplexer config and themes.

## Contents
- `config.kdl` — Main Zellij config.
- `themes/catppuccin.kdl` — KDL theme file.
- `themes/catppuccin.yaml` — Theme metadata/variant.

## Current Stow target result
- `~/.config/config.kdl`
- `~/.config/themes/...`

## Usage status
- Potentially mis-targeted. Zellij usually expects `~/.config/zellij/config.kdl` and namespaced themes.
