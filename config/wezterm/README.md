# wezterm/

## Purpose
WezTerm terminal configuration.

## Contents
- `wezterm.lua` — Main WezTerm config.

## Current Stow target result
- `~/.config/wezterm.lua`

## Usage status
- Potentially mis-targeted; many setups use `~/.wezterm.lua` or `~/.config/wezterm/wezterm.lua`.
