# zshrc/

## Purpose
Z shell startup configuration.

## Contents
- `.zshrc` — Shell initialization and aliases/functions.

## Current Stow target result
- `~/.config/.zshrc`

## Usage status
- Likely unused by default Zsh, which expects `~/.zshrc` in home directory.
